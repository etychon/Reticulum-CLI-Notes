# Config snippets

Only **redacted** or **dummy** interface blocks belong here. Do not commit live destinations, keys, or callsign-specific RF plans.

Files use illustrative names (e.g. `example-tcp-client.toml`). Operators must verify frequency, bandwidth, spreading factor, and transmit power against **local regulations** and hardware capabilities.
